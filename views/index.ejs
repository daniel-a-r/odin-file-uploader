<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head') %>
  <body>
    <%- include('./partials/nav') %>
    <h1>Hello World</h1>
    <% if (locals.currentUser) { %>
      <h2>Currently logged in as <%= locals.currentUser.username %></h2>
    <% } %>
  </body>
</html>
